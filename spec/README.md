# Spec Folder

This folder is reserved for the first technical draft of the Agent Identity Layer.

Current contents:

- `agent-identity-envelope.v0.md` — first technical draft of the core envelope
- `../examples/single-agent.local.json` — direct/local example
- `../examples/delegated-coding-agent.json` — delegated agent example
- `../examples/tool-call.signed-placeholder.json` — signed-placeholder example

Current status: documentation-first v0 with a practical envelope draft, examples, and verification notes. A formal JSON Schema can follow once field boundaries settle.
